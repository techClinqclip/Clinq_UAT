`python manage.py runserver` ==> Django backemd server at port 8000.

`celery -A core worker --loglevel=INFO` ==> Background worker server.

`redis-cli` PING ==> Redis server status at 6379.

`npm run dev` ==> frontend server at 5000 in the cling-frontend Directory.