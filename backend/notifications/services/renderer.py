class NotificationRenderer:
    def render(event, channel):
        if channel == "IN_APP":
            return {...}
        if channel == "EMAIL":
            return {...}
        if channel == "PUSH":
            return {...}
