# TODO

## Brand Dashboard: connect to real DB data
- [ ] Update `frontend/src/pages/Brand/Dashboard.jsx` to replace hardcoded KPI/top/recent campaigns with data fetched from `GET /api/content/campaigns/` and aggregated client-side.
- [ ] Verify campaign fields mapping (name/status/views/budget).
- [ ] Ensure loading/error states render correctly.
- [ ] Smoke test by running frontend and visiting `/brand/dashboard`.

